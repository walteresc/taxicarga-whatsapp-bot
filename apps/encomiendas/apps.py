from django.apps import AppConfig


class EncomiendasConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.encomiendas"
    verbose_name = "Encomiendas — envíos door-to-door"
