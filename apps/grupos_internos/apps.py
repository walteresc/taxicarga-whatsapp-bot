from django.apps import AppConfig


class GruposInternosConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.grupos_internos"
    verbose_name = "Grupos internos (chat de equipo)"
