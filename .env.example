# Django Settings
DJANGO_SETTINGS_MODULE=config.settings_e2e
DEBUG=False
SECRET_KEY=change-me-to-a-real-secret-key-min-50-chars-XXXXXXXXXXXXXXXXXXXX

# Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=taxicarga_pg_test
DB_USER=taxicarga
DB_PASSWORD=

# Redis
REDIS_URL=redis://redis:6379/0

# YCloud Webhook
YCLOUD_WEBHOOK_SECRET=your-ycloud-webhook-secret

# Allowed Hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1,nginx

# Email (if configured)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# HTTPS (for production with Cloudflare)
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
